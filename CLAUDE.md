# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a minimal Go project named "getakey" that appears to be in its initial setup phase. The project uses Go 1.24.4 and currently contains only the module definition.

## Development Commands

Since this is a new Go project with minimal setup, standard Go commands apply:

- `go run .` - Run the main package (once main.go is created)
- `go build` - Build the project
- `go test ./...` - Run all tests
- `go mod tidy` - Clean up module dependencies
- `go fmt ./...` - Format Go code
- `go vet ./...` - Run Go vet for static analysis

## Architecture

The project structure is currently minimal with only:
- `go.mod` - Go module definition for the "getakey" module

This appears to be a fresh Go project ready for development. The module name suggests it may be related to key management or retrieval functionality.